import { MessageFlags } from 'discord.js'; import { CONFESSION,getConfession,saveConfession } from '../../services/confessionService.js';
export default {name:'confession_follow',async execute(interaction,client,args){const d=await getConfession(client,args[0]);if(!d)return interaction.reply({content:'❌ Không tìm thấy confession.',flags:MessageFlags.Ephemeral});d.followers ||= [];const i=d.followers.indexOf(interaction.user.id);let following;if(i>=0){d.followers.splice(i,1);following=false}else{d.followers.push(interaction.user.id);following=true}await saveConfession(client,d);await interaction.reply({content:following?`${CONFESSION.emojiFollow} **Đã theo dõi Confession #${d.number}**\nBạn sẽ nhận được thông báo khi confession này có câu trả lời mới.`:`${CONFESSION.emojiFollow} **Đã bỏ theo dõi Confession #${d.number}**`,flags:MessageFlags.Ephemeral});}};