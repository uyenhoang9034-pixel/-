import { MessageFlags,ModalBuilder,TextInputBuilder,TextInputStyle,ActionRowBuilder } from 'discord.js'; import { CONFESSION,getConfession } from '../../services/confessionService.js';
export default {name:'confession_reject',async execute(interaction,client,args){if(!interaction.member?.roles?.cache?.has(CONFESSION.reviewerRoleId))return interaction.reply({content:'❌ Bạn không có role duyệt confession.',flags:MessageFlags.Ephemeral});const d=await getConfession(client,args[0]);if(!d||d.status!=='pending')return interaction.reply({content:'ℹ️ Confession này đã được xử lý hoặc không tồn tại.',flags:MessageFlags.Ephemeral});const input=new TextInputBuilder().setCustomId('reason').setLabel('Lý do').setPlaceholder('Nhập lý do từ chối...').setStyle(TextInputStyle.Paragraph).setRequired(true).setMaxLength(1000);await interaction.showModal(new ModalBuilder().setCustomId(`confession_reject_submit:${d.id}`).setTitle(`Từ chối Confession #${d.number}`).addComponents(new ActionRowBuilder().addComponents(input)));}};