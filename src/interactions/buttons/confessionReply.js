import { ModalBuilder,TextInputBuilder,TextInputStyle,ActionRowBuilder } from 'discord.js';
export default {name:'confession_reply',async execute(interaction,client,args){const input=new TextInputBuilder().setCustomId('content').setLabel('Nội dung trả lời').setPlaceholder('Nhập câu trả lời của bạn...').setStyle(TextInputStyle.Paragraph).setMaxLength(4000).setRequired(true);await interaction.showModal(new ModalBuilder().setCustomId(`confession_reply_submit:${args[0]}`).setTitle('Trả lời Confession').addComponents(new ActionRowBuilder().addComponents(input)));}};